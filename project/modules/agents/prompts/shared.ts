export const engineeringPolicy = `ENGINEERING POLICY
==================
- Prefer the smallest correct change.
- Reuse existing repository patterns before introducing new ones.
- Avoid unnecessary abstractions, layers, or indirection.
- Keep changes focused on the requested scope.
- Update tests when behavior changes or the repo already uses tests for that area.
- Favor readability, explicit control flow, and maintainability over cleverness.
- If multiple approaches are viable, choose the one with the least surface area and lowest risk.
- Do not refactor unrelated code.
- Do not add dependencies unless clearly required.
- When the repository already solves a similar problem, follow that pattern unless there is a strong reason not to.`;

