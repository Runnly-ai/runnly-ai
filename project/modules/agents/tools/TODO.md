# Tool Roadmap

- [ ] Add `agent_delegate` for subagent spawning and task offload.
- [ ] Add richer `grep`/`search` output modes with context and limits.
- [ ] Add structured file edit operations with diff-aware workflows.
- [ ] Add command/result caching and retry suppression for repeated failures.
- [ ] Add optional web/search integrations only if a task explicitly needs network access.
